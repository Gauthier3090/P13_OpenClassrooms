setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: bucket-oc-lettings
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: development
release:
  command:
    - ./deployment-tasks.sh
  image: web
